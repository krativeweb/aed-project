export const metadata = {
    title: "Login | AED Locate",
    // description: "Register an AED device and contribute to the AED Locator network.",
}
  
  export default function Login({ children }) {
    return <>{children}</>;
  }
  